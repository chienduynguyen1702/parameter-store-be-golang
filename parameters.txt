CERT_FILE_PATH=param-store-be.datn.live.crt
COOKIE_DOMAIN=https://param-store-be.datn.live
DB_DATABASE=param
DB_HOST=parameter-store-db
DB_PASSWORD=postgres
DB_PORT=5432
DB_URL=postgres://${db_username}:${db_password}@${db_host}:${db_port}/${db_database}?sslmode=disable
DB_USERNAME=postgres
ENABLE_HEALTH_CHECK=true
ENABLE_HTTPS_LOCAL=true
ENABLE_SWAGGER=true
ENVIRONMENT=datn-server
GIN_MODE=release
HOSTNAME="param-store-be.datn.live"
HOSTNAME_URL=https://param-store-be.datn.live
KEY_FILE_PATH=server.key
PORT=8080
RUN_MIGRATION=true
RUN_SEED=false
SECRET_KEY=secret
SERVERLESS_DEPLOY=true
SWAGGER_HOST=https://param-store-be.datn.live
SWAGGER_SCHEME=https
